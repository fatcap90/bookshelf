import React from "react";

const Book = (props) => {
    return (
        <div className="book">

                <div className="book-title">
                    Название книги
                </div>
        </div>
    );
};

export default Book;
